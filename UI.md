add below in package.json then use "SAP Fiori Generator to create a Fiori app"

"cds": {
    "requires": {
      "db": "sqlite"
    }
  },

  cds deploy --to sqlite

  npm run watch-processorservice


  then the UI came